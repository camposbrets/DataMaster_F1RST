name: 'capag'
version: '2.0.0'
config-version: 2

profile: 'capag'

models:
  capag:
    bronze:
      +materialized: view
      +schema: bronze
      +tags: ['bronze']
    silver:
      +materialized: table
      +schema: silver
      +tags: ['silver']
    gold:
      +materialized: table
      +schema: gold
      +tags: ['gold']

vars:
  projeto_id: 'projeto-data-master'
